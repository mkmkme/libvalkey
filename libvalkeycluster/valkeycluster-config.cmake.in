@PACKAGE_INIT@

set_and_check(valkeycluster_INCLUDEDIR "@PACKAGE_INCLUDE_INSTALL_DIR@")

if(NOT TARGET valkeycluster::valkeycluster)
  include(${CMAKE_CURRENT_LIST_DIR}/valkeycluster-targets.cmake)
endif()

set(valkeycluster_LIBRARIES valkeycluster::valkeycluster)
set(valkeycluster_INCLUDE_DIRS ${valkeycluster_INCLUDEDIR})

check_required_components(valkeycluster)

