@PACKAGE_INIT@

set_and_check(valkeycluster_ssl_INCLUDEDIR "@PACKAGE_INCLUDE_INSTALL_DIR@")

if(NOT TARGET valkeycluster::valkeycluster_ssl)
  include(${CMAKE_CURRENT_LIST_DIR}/valkeycluster_ssl-targets.cmake)
endif()

set(valkeycluster_ssl_LIBRARIES valkeycluster::valkeycluster_ssl)
set(valkeycluster_ssl_INCLUDE_DIRS ${valkeycluster_ssl_INCLUDEDIR})

check_required_components(valkeycluster_ssl)
